#include <stdio.h>
#include <stdlib.h>

int main() {
    int x = 8;
    printf("x = %d\n", x);
    if (x>5) {
        printf("x is > 5\n");
    }
    if (x<5) {
        printf("x is < 5\n");
    }
    system("pause");
    return 0;
}